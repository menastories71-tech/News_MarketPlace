# ==========================================================
# CRAWLER DETECTION MAP
# ==========================================================
# This MUST be placed OUTSIDE the server blocks (in the http context)
# Put this at the TOP of your /etc/nginx/nginx.conf or at the top of 
# your site-available config if your nginx version allows it locally.
# ==========================================================
map $http_user_agent $is_crawler {
    default                     0;
    ~*facebookexternalhit       1;
    ~*Facebot                   1;
    ~*Twitterbot                1;
    ~*LinkedIn                  1;
    ~*LinkedInBot               1;
    ~*WhatsApp                  1;
    ~*SkypeUriPreview           1;
    ~*Slackbot                  1;
    ~*TelegramBot               1;
    ~*Discordbot                1;
    ~*Pinterest                 1;
    ~*Googlebot                 1;
    ~*bingbot                   1;
}

# ==========================================================
# UPDATE YOUR SERVER BLOCK AS SHOWN BELOW
# ==========================================================
server {
    listen 80;
    server_name vaas.solutions www.vaas.solutions;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl; # Add your SSL config here
    server_name vaas.solutions www.vaas.solutions;

    # Serve frontend build files
    root /var/www/html/news-marketplace;
    index index.html;

    # ==========================
    # CRAWLER ROUTES (DYNAMIC SHARING)
    # ==========================
    # We use a direct User-Agent check to ensure bots are ALWAYS proxied to the metadata service.
    # This prevents them from hitting the static index.html.
    location ~* ^/(publications|events|blog|blogs|careers|themes|power-lists|paparazzi|awards|real-estate-professionals|radio|podcasters|press-packs) {
        
        # Explicit bot detection list
        if ($http_user_agent ~* (facebookexternalhit|Facebot|LinkedIn|LinkedInBot|Twitterbot|WhatsApp|Slack|Discord|Telegram|Googlebot|bingbot|Applebot|bot|crawler|spider)) {
            proxy_pass http://127.0.0.1:3000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # CRITICAL: Force Nginx to ignore Range requests from LinkedIn (Fixes 206 Success)
            proxy_set_header Range ""; 
            proxy_set_header If-Range "";
            
            # Disable caching for crawlers so they always get fresh DB metadata
            proxy_cache_bypass 1;
            proxy_no_cache 1;
            
            # Disable gzip for crawlers as it can sometimes cause encoding issues with scrapers
            gzip off;
            
            break;
        }

        # For humans/browsers: serve the React app
        try_files $uri $uri/ /index.html;
    }
    
    

    # ==========================
    # GENERAL ROUTES
    # ==========================

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Reverse proxy to backend
    location /api/ {
        proxy_pass http://127.0.0.1:3000/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /api/translation/ {
        proxy_pass http://127.0.0.1:5005/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location ~* \.(?:manifest|appcache|html?|xml|json)$ {
        expires -1;
    }

    location ~* \.(?:css|js|jpg|jpeg|gif|png|svg|ico|woff2?|ttf)$ {
        expires 7d;
        add_header Cache-Control "public";
    }

    access_log /var/log/nginx/news-marketplace.access.log;
    error_log /var/log/nginx/news-marketplace.error.log;
}
